[project]
name = "data-collector"
version = "0.1.0"
description = "SEL children resource data collector"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
