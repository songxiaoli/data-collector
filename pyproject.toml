[project]
name = "data-collector"
version = "0.1.0"
description = "数据采集项目"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
